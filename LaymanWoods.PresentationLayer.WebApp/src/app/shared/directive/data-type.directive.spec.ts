import { DataTypeDirective } from './data-type.directive';

describe('DataTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTypeDirective();
    expect(directive).toBeTruthy();
  });
});
